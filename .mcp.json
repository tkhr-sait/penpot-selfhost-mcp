{
  "mcpServers": {
    "penpot-official": {
      "command": "docker",
      "args": [
        "compose", "-f", ".claude/skills/penpot/scripts/mcp-proxy/docker-compose.yml",
        "run", "--build", "--rm", "-T", "penpot-proxy",
        "--upstream=http://localhost:4401/sse"
      ]
    },
    "storybook-mcp": {
      "command": "docker",
      "args": [
        "compose", "-f", ".claude/skills/penpot/scripts/mcp-proxy/docker-compose.yml",
        "run", "--build", "--rm", "-T", "storybook-proxy",
        "--upstream=http://localhost:6007/sse"
      ]
    }
  }
}
